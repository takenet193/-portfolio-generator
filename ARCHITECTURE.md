# アーキテクチャ

## 概要

Portfolio Generatorは、コードベースから情報を抽出し、AIを活用してExcelベースのポートフォリオを自動生成するシステムです。3段階のワークフロー（プロンプト生成 → AI生成 → Excel出力）を採用し、柔軟性と拡張性を重視した設計になっています。

## システムアーキテクチャ

### 全体像

```
┌─────────────────────────────────────────────────────────────┐
│                     Portfolio Generator                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐ │
│  │  設定読み込み  │───▶│ コードベース  │───▶│ プロンプト    │ │
│  │              │    │ 解析         │    │ 生成         │ │
│  └──────────────┘    └──────────────┘    └──────────────┘ │
│         │                    │                    │          │
│         │                    │                    ▼          │
│         │                    │          ┌──────────────┐   │
│         │                    │          │ AI生成       │   │
│         │                    │          │ (外部)       │   │
│         │                    │          └──────────────┘   │
│         │                    │                    │          │
│         │                    │                    ▼          │
│         │                    │          ┌──────────────┐   │
│         │                    └─────────▶│ Excel出力    │   │
│         │                               └──────────────┘   │
│         │                                                    │
│         └───────────────────────────────────────────────────┘
│
└─────────────────────────────────────────────────────────────┘
```

## 主要コンポーネント

### 1. 設定管理層

#### `config_loader.py` - PortfolioConfig
- **役割**: 設定ファイル（`.cursor/portfolio_config.json`）の読み込みと検証
- **主要機能**:
  - JSON設定ファイルのパース
  - セルマッピング情報の管理
  - Excelテンプレート情報の管理
  - バリデーション

#### `user_settings.py` - UserSettings
- **役割**: ユーザー設定ファイル（`docs/portfolio-user-settings.md`）の読み込み
- **主要機能**:
  - Markdown形式の設定ファイルのパース
  - セルごとの文字数制限のオーバーライド
  - AIへの追加指示の管理

### 2. コードベース解析層

#### `file_discovery.py` - ファイル探索
- **役割**: プロジェクト構造の自動認識と関連ファイルの探索
- **主要機能**:
  - パターンマッチングによるドキュメントファイルの検出
  - プロジェクト構造の自動認識（src/, docs/, など）
  - ファイル重要度のスコアリング
  - 優先度に基づくファイル選択

#### `codebase_reader.py` - コードベース読み込み
- **役割**: ファイルの読み込みとコンテキスト構築
- **主要機能**:
  - ファイル内容の読み込み（最大10MB制限）
  - メタデータの管理（ファイルサイズ、行数など）
  - コンテキスト情報の構造化

#### `git_history.py` - Git履歴解析
- **役割**: Gitリポジトリの履歴情報の取得と解析
- **主要機能**:
  - コミット履歴の取得
  - 日付ごとのグループ化
  - 変更ファイルのカテゴリ分類
  - 開発統計情報の生成

#### `semantic_search.py` - セマンティック検索
- **役割**: セマンティック検索のインターフェース提供
- **主要機能**:
  - 検索クエリの生成
  - Cursor AIのcodebase_searchとの連携
  - 検索結果の解析

### 3. プロンプト生成層

#### `prompt_generator.py` - PromptGenerator
- **役割**: 各セルごとの詳細なプロンプトファイルの生成
- **主要機能**:
  - セルごとのプロンプトファイル生成
  - コードベースコンテキストの組み込み
  - Git履歴の自動読み込みと組み込み
  - ユーザー設定の反映
  - 多言語・トーン設定の反映

### 4. 内容生成層

#### `content_generator.py` - ContentGenerator
- **役割**: 内容生成の簡易実装（現在は使用されていません）
- **主要機能**:
  - テキスト変換ルールの適用
  - 文字数制限の適用
  - 基本的な要約機能

**注意**: 実際のAI生成はCursor AIなどの外部ツールを使用します。

### 5. データ管理層

#### `content_storage.py` - JSON保存・読み込み
- **役割**: 生成された内容のJSON形式での保存・読み込み
- **主要機能**:
  - `.cursor/portfolio-generated-content.json`の読み書き
  - セルデータの構造化
  - バージョン管理

### 6. Excel出力層

#### `excel_writer.py` - ExcelWriter
- **役割**: Excelファイルの操作と書き込み
- **主要機能**:
  - テンプレートファイルのコピー
  - セルへの内容書き込み
  - 書式の保持
  - 結合セルへの対応

### 7. エントリーポイント

#### `generate_portfolio_v2.py` - メインスクリプト
- **役割**: CLIエントリーポイントとワークフロー制御
- **主要機能**:
  - コマンドライン引数の解析
  - 3つのモードの実装:
    1. プロンプト生成モード（`--generate-prompts`）
    2. JSON読み込みモード（`--from-json`）
    3. 通常モード（簡易実装）
  - エラーハンドリングとロギング

## データフロー

### プロンプト生成フェーズ

```
1. 設定ファイル読み込み
   portfolio_config.json
   └─▶ PortfolioConfig
       └─▶ セルマッピング情報

2. コードベース解析
   プロジェクトルート
   ├─▶ file_discovery.py (ファイル探索)
   ├─▶ codebase_reader.py (ファイル読み込み)
   ├─▶ git_history.py (Git履歴取得)
   └─▶ CodebaseContext (コンテキスト構築)

3. プロンプト生成
   PortfolioConfig + CodebaseContext + UserSettings
   └─▶ prompt_generator.py
       └─▶ docs/portfolio-prompts/*.md (プロンプトファイル)
```

### AI生成フェーズ（外部）

```
docs/portfolio-prompts/*.md
└─▶ Cursor AI (外部ツール)
    └─▶ .cursor/portfolio-generated-content.json
```

### Excel出力フェーズ

```
.cursor/portfolio-generated-content.json
└─▶ content_storage.py (JSON読み込み)
    └─▶ excel_writer.py
        └─▶ ポートフォリオ.xlsx (出力ファイル)
```

## 設計思想

### 1. モジュール化

各機能を独立したモジュールとして実装し、以下の利点を実現：

- **保守性**: 各モジュールが独立しているため、変更の影響範囲が限定的
- **テスト容易性**: 各モジュールを個別にテスト可能
- **再利用性**: 他のプロジェクトでもモジュールを再利用可能

### 2. 設定駆動型アーキテクチャ

設定ファイル（JSON、Markdown）で動作を制御：

- **柔軟性**: コードを変更せずに動作をカスタマイズ可能
- **汎用性**: 様々なプロジェクトで同じコードベースを使用可能
- **保守性**: 設定とコードが分離されているため、理解しやすい

### 3. 段階的な処理

3段階のワークフローにより、各フェーズを独立して実行可能：

- **デバッグ容易性**: 各フェーズで中間結果を確認可能
- **再実行性**: 特定のフェーズだけを再実行可能
- **拡張性**: 新しいフェーズを追加しやすい

### 4. エラーハンドリング

部分的な失敗を許容する設計：

- **堅牢性**: 一部のファイルが読み込めなくても処理を継続
- **ユーザビリティ**: エラーがあっても可能な限り結果を出力

## 技術スタック

### コア技術

- **Python 3**: メイン言語
- **openpyxl**: Excelファイル操作
- **標準ライブラリ**: ファイル操作、JSON処理、ロギングなど

### 外部依存

- **Cursor AI**: セマンティック検索とAI生成（オプション）
- **Git**: Git履歴の取得（オプション）

### 設定ファイル形式

- **JSON**: メイン設定ファイル（`.cursor/portfolio_config.json`）
- **Markdown**: ユーザー設定ファイル（`docs/portfolio-user-settings.md`）

## 拡張性

### 将来の拡張ポイント

1. **ベクトルデータベース連携**
   - `semantic_search.py`を拡張し、Chroma、PineconeなどのベクトルDBと連携
   - 高速なセマンティック検索の実装

2. **埋め込み（Embedding）の実装**
   - テキストをベクトル化する機能の追加
   - 意味的類似度に基づく検索の実装

3. **チャンキング戦略**
   - 長文を適切なサイズに分割する機能
   - 大規模ドキュメント処理への対応

4. **評価指標の実装**
   - RAGシステムの品質を測る指標（精度、再現率、応答時間など）
   - プロダクション品質の実現

5. **テンプレートなし生成**
   - Excelテンプレートがなくても、まっさらなブックから必要なシート・セル構造を自動生成

6. **複数プロジェクト対応**
   - 複数のプロジェクトを一度に処理
   - プロジェクト間の比較機能

## パフォーマンス考慮事項

### ファイル読み込み

- **最大ファイルサイズ**: 10MB（`codebase_reader.py`）
- **優先度付け**: 重要度の高いファイルのみを読み込み（`file_discovery.py`）
- **最大ファイル数**: デフォルト10ファイル（設定可能）

### メモリ管理

- **ストリーミング処理**: 大きなファイルは部分的に読み込み
- **コンテキストの効率的な管理**: 必要な情報のみを保持

## セキュリティ考慮事項

### ファイルアクセス

- **パス検証**: 相対パスの解決と検証
- **権限チェック**: ファイル読み込み権限の確認

### 設定ファイル

- **バリデーション**: 設定ファイルの形式と内容の検証
- **エラーハンドリング**: 不正な設定に対する適切なエラー処理

## まとめ

Portfolio Generatorは、**モジュール化された設計**、**設定駆動型アーキテクチャ**、**段階的な処理**を特徴とする、柔軟で拡張性の高いシステムです。RAG技術の実践的な理解を示しつつ、実用的なツールとして完成度の高い実装になっています。
